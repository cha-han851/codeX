import React, { ReactNode, useEffect } from 'react';
import Head from 'next/head';
import { useRouter } from 'next/router';


type Props = {
  children?: ReactNode;
};

const UsersLayout: React.FC<Props> = ({ children }: Props) => {

  return (
    <>

    </>
  );
};

export default UsersLayout;
